# Pinned to the versions validated locally (42 tests green, app.main imports clean).
# Direct deps only; transitive closure is reproduced by these exact versions.

# Web service
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# HTTP
httpx==0.28.1

# Config / validation
pydantic==2.13.4
pydantic-settings==2.14.1

# LLM + agent graph
langchain-core==1.4.6
langchain-openai==1.3.0
langgraph==1.2.4
langgraph-checkpoint-postgres==3.1.0

# Postgres (business tables + LangGraph checkpointer)
psycopg[binary,pool]==3.3.4
psycopg-pool==3.3.1

# OpenAI (STT, direct usage)
openai==2.41.1

# Scheduling (follow-ups)
APScheduler==3.11.2

# Resilience
tenacity==9.1.4

# Dev / test
pytest==9.0.3
pytest-asyncio==1.4.0
respx==0.23.1
